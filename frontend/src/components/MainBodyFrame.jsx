import styled from "styled-components";

function MainBodyFrame(props) {
    return (
        <styled.MainBodyFrame>
            {props.content}
        </styled.MainBodyFrame>
    );
}

export default MainBodyFrame;